{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Magic Ontology",
  "description": "A flexible JSON schema defining the properties of magical spells, items, or effects within the world.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the magic."
    },
    "wieldability": {
      "type": "string",
      "enum": ["innate", "learned", "item_bound", "ritual", "environmental"],
      "description": "How the magic is wielded or acquired."
    },
    "cost": {
      "type": "number",
      "minimum": 0,
      "description": "The resource cost (mana, stamina, health, etc.) required to use the magic."
    },
    "effectType": {
      "type": "string",
      "enum": ["creation", "destruction", "alteration", "mind", "healing", "movement"],
      "description": "The primary category of the magic's effect."
    }
  },
  "required": ["name", "wieldability", "cost", "effectType"],
  "additionalProperties": true
}
